{
  "name": "cyber-chat",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/public && mv frontend/dist backend/public",
    "start": "npm install --prefix backend && npm run start --prefix backend"
  }
}
