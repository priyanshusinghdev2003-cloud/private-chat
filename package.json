{
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/public && cp -R frontend/dist backend/public",
    "start": "npm install --prefix backend && npm start --prefix backend"
  }
}

